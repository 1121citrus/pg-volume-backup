#!/usr/bin/env bash
# shellcheck shell=bash

# test/staging-hooks.sh — repo-specific helpers for the pg-volume-backup
# staging harness (test/staging).
#
# Called by: test/staging (generated) via `source staging-hooks.sh`
#
# pg-volume-backup staging relies entirely on the generated harness
# (scan/advise tests).  No repo-specific test functions are needed.
